

/*
function Burger(prod, kC, glu, lac, img) {
  this.name = prod;
  this.kCal = kC;
  this.gluten = glu;
  this.lactose = lac;
  this.image = img;

  }
*/

// let menu = [burg1, burg2, burg3]; //menu array

// burgermenu is the menu-elemnt in the html
// var myElement = document.getElementById("myID");
// for (let i = 0; i<menu.length;i++) {
  // var burger = menu[i];
  // var energy = document.createTextNode(burger.kCal);
  // var burgerName = document.createTextNode(burger.name);
  //burgerName.appendChild(energy);
  // myElement.appendChild(burgerName);
  // myElement.appendChild(energy);

// }
